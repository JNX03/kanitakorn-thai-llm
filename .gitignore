# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Project-specific exclusions

# 1. Regenerable artifacts (skip in git — produced by tools/)
tools/__pycache__/
tools/_locked_snapshot/
tools/.judge_cache/
.judge_cache/
.backup_pre_rebalance_*.tgz

# 2. Large benchmark inputs (1.97GB) — regenerated by
#    `python tools/benchmark_eval.py --inputs-only dataset/reports/benchmark_inputs.jsonl --family all`
#    (takes ~5 min from HuggingFace cache on the A100)
dataset/reports/benchmark_inputs.jsonl

# 3. Trained model checkpoints (will be huge if produced locally)
runs/
checkpoints/
*.safetensors
*.bin

# 4. HF cache (if user accidentally puts it here)
.cache/
.env.secrets
_state/
# Old runscripts with embedded secrets (kept local, do not commit)
RUN_24H_PLAN.md
fire_24h_when_ready.sh
FIRE_ON_SSH.sh
post_r6_pipeline.sh
train_round9.sh
train_simpo_r7.sh
